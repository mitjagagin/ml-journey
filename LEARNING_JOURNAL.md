# 📚 Learning Journal: ML/AI Engineering Journey

**Студент:** Дмитрий
**Цель:** Junior/Middle AI Engineer
**Стэк:** Windows, PyCharm, Miniconda, Git, Docker

---

## 📝 Лог прогресса

### [2026-08-08] Этап 0: Завершение Главы 5 — if Statements (Matthes 5.3.4 - 5.4.2)
- **Изучено (Python/ML):**
  - **Несколько `elif` блоков (5.3.4):** каждый `elif` проверяет отдельное условие, Python выполняет **первый** истинный блок. Порядок условий важен — сверху вниз.
  - **Пропуск блока `else` (5.3.5):** блок `else` не обязателен; можно писать только явные условия. Если все условия ложны и `else` нет — код просто пропускается.
  - **Проверка нескольких условий (5.3.6):** последовательные `if` блоки проверяют все условия независимо (в отличие от `if-elif-else`, где срабатывает только первый).
  - **Проверка особых элементов в цикле (5.4.1):** комбинация `for` + `if` для поэлементной валидации списка — основа обработки данных в пайплайнах.
  - **Проверка, что список не пустой (5.4.2):** `if list_name:` использует истинность списка (пустой = `False`, непустой = `True`). Не нужно `len()` или сравнение с `[]`.
- **Инженерные практики:**
  - **Строгая логика отметок в `COURSE_PLAN.md`:** родительский пункт `[x]` только когда **все** дочерние пункты `[x]`. Если хотя бы один дочерний `[ ]` — родитель тоже `[ ]`.
  - **Шаги 61-65:** созданы 5 учебных скриптов согласно стандарту 8.5:
    - `step_61_multiple_elif_blocks.py`
    - `step_62_omitting_else_block.py`
    - `step_63_testing_multiple_conditions.py`
    - `step_64_checking_for_special_items.py`
    - `step_65_checking_list_not_empty.py` (код готов, **коммит ждёт**)
  - **Обновление `COURSE_PLAN.md`:** пункты 5.3.4, 5.3.5, 5.3.6, 5.4.1, 5.4.2 переведены в `[x]`. Пункт 5.4 остаётся `[ ]` (строгая логика: 5.4.3 не пройден).
- **Связь с ML/AI (Зачем это нужно):**
  - **Несколько `elif`** — градация уровней важности в мониторинге: `critical` → стоп пайплайн, `error` → алерт дежурному, `warning` → запись в лог (Huyen DMLS, Ch. 7 — monitoring & alerting).
  - **Пропуск `else`** — обработка только известных значений: если статус модели не в whitelist (`ready`, `training`, `failed`) — код ничего не делает, а не пытается обработать неизвестное (McMahon Ch. 5 — deployment gates).
  - **Последовательные `if`** — мульти-критериальная валидация: проверка accuracy, latency, memory usage — каждый критерий оценивается независимо, все должны пройти (Huyen DMLS, Ch. 3 — data validation).
  - **`for` + `if`** — поэлементная фильтрация данных: цикл проходит по каждому значению датасета, условие определяет — принять, пометить как выброс или отбросить (Huyen DMLS, Ch. 3 — data preprocessing).
  - **`if list_name:`** — проверка наличия данных перед обработкой: "если батч пуст — пропустить итерацию", "если список предсказаний пуст — не запускать агрегацию метрик" (McMahon Ch. 3 — training pipelines).
- **Ссылка на коммиты:** `feat(python): add step 61...` — `feat(python): add step 64...` (закоммичены и запушены); `step_65` — код готов, коммит ждёт.

### [2026-08-08] Этап 0: Освоение условных инструкций (Matthes Глава 5 — if Statements, разделы 5.1 - 5.3.3)
- **Изучено (Python/ML):**
  - **Операторы сравнения:** `==`, `!=`, `>`, `<`, `>=`, `<=` — основа всех условных проверок в коде.
  - **Проверка равенства строк:** чувствительность к регистру, сравнение `==` вне `if` (возвращает `True`/`False` как обычное значение).
  - **Игнорирование регистра:** метод `.lower()` для приведения строк к единому виду перед сравнением. Строки в Python **неизменяемы** — `.lower()` создаёт новую строку.
  - **Логические операторы:** `and` (строгий привратник — нужны все условия), `or` (мягкий — достаточно одного).
  - **Операторы принадлежности:** `in` и `not in` — проверка наличия/отсутствия элемента в списке (работают также со строками и словарями).
  - **Булевы выражения:** сохранение результата сравнения (`True`/`False`) в переменную (`bool`) для повторного использования.
  - **Три паттерна условных инструкций:**
    - Простой `if` без `else` — осознанный пропуск действия (side-effects: логирование, алерты).
    - `if-else` — обязательное ветвление (маршрутизация пайплайна: деплой или переобучение).
    - `if-elif-else` — многоуровневая маршрутизация (выбор действия из нескольких вариантов).
- **Инженерные практики:**
  - **Обновление `mentor_prompt.md`:** добавлен раздел 8.6 «Стандарт объяснения новых концепций» — обязательная таблица `Конструкция | Что делает` после кода каждого микро-шага + блок «Логика по шагам».
  - **Возврат подсветки синтаксиса:** блоки кода снова оформляются как ` ```python ` (с указанием языка), а не через отступы. Правило безопасности: если документ сам содержит блоки кода — оборачивать его в четыре обратные кавычки.
  - **Синхронизация `COURSE_PLAN.md`:** 13 пунктов Главы 5 (5.1, 5.2.x, 5.3.1-5.3.3) переведены из `[ ]` в `[x]`.
  - **Извлечённый урок:** всегда сверяться со списком пройденных концепций перед выдачей нового шага — в таблице 8.6 только ТО, что появляется впервые. Избегать повторов (как с `else` в шаге 59, который уже был в шаге 49).
  - **Шаги 49-60:** созданы 12 учебных скриптов (`step_49_simple_if_example.py` ... `step_60_if_elif_else_chain.py`) согласно стандарту 8.5 (нейминг, docstring, ML-контекст, формат вывода без `---`).
- **Связь с ML/AI (Зачем это нужно):**
  - **`==` / `!=`** — сравнение предсказанных меток с истинными (основа расчёта accuracy), проверка статусов пайплайна (`status == "completed"`), валидация форматов данных.
  - **`.lower()`** — стандартная техника очистки текстовых данных (NLP): приведение к единому регистру перед токенизацией и векторизацией (Huyen DMLS, Ch. 3 — data preprocessing).
  - **`and` / `or`** — основа quality gates в CI/CD: деплой только если `accuracy >= target AND latency <= threshold`; переобучение если `accuracy < target OR drift_detected` (McMahon Ch. 5, Gift Ch. 5 — monitoring).
  - **`in` / `not in`** — whitelist/blacklist фильтрация: валидация категориальных признаков, проверка класса ∈ допустимому набору, guardrails в LLM-системах (Iusztin Ch. 8 — safety).
  - **Boolean-флаги** (`is_ready`, `has_error`, `is_validated`) — стандартный паттерн управления потоком в ML-пайплайнах: вычислить один раз, использовать везде.
  - **`if-elif-else`** — многоуровневая маршрутизация моделей: `accuracy >= 95 → деплой`, `>= 90 → дообучение`, иначе → полный ретрейн (McMahon Ch. 5 — deployment gates).
- **Ссылка на коммиты:** серия атомарных коммитов `feat(python): add step NN ...` (шаги 49-58 закоммичены и запушены; шаги 59-60 — код создан, коммиты в процессе).

### [2026-08-06] Этап 0: Завершение Главы 4 — Копирование списков, кортежи и стиль кода (Matthes 4.4.3 - 4.6)
- **Изучено (Python/ML):** 
  - Копирование списков через срез `[:]` против простого присваивания `=`. Понимание разницы между созданием независимой копии и созданием ссылки на тот же объект в памяти.
  - Кортежи (tuples): неизменяемые списки, синтаксис `(val1, val2)`, итерация по кортежам, переопределение переменной кортежа целиком.
  - Стиль кода (PEP 8): именование переменных в snake_case, использование пустых строк для разделения логических блоков, важность читаемости кода.
  - Внедрение строгого стандарта оформления учебных скриптов: нейминг `step_NN_snake_case_name.py`, многострочные docstring, форматирование вывода `print()`.
- **Инженерные практики:** 
  - Обновление системного промпта (`mentor_prompt.md`): добавлен раздел 8.5 с жестким стандартом оформления учебных скриптов (tutorials/) и эталонным примером.
  - Строгое следование стандарту PEP 8 и новому формату оформления файлов без отклонений.
  - Атомарные коммиты и push каждого учебного скрипта (`step_46`, `step_47`, `step_48`) согласно `GIT_WORKFLOW.md`.
- **Связь с ML/AI (Зачем это нужно):** 
  - Копирование списков `[:]`: критически важно для предотвращения data leakage и случайной мутации "сырых" данных (raw data) при применении трансформаций (аугментации, нормализация) к батчам.
  - Кортежи: используются для хранения неизменяемых конфигураций, гиперпараметров модели, размеров изображений (dimensions) и координат, защищая их от случайного изменения в пайплайне.
  - Стиль кода и стандарты оформления: в продакшене код читают чаще, чем пишут. Единый стиль (PEP 8) необходим для code review, командной работы и поддержки сложных ML-систем.
- **Ссылка на коммит:** `feat(python): complete matthes chapter 4 (copying lists, tuples, styling code); docs: add tutorial formatting standard`

### [2026-08-03] Этап 0: Статистика списков, List Comprehensions, Slicing и итерация по срезу (Matthes 4.3.3 - 4.4.1)
- **Изучено (Python/ML):** 
  - Встроенные функции статистики: `min()`, `max()`, `sum()` для анализа числовых списков.
  - Функция `round(value, ndigits)`: корректное округление чисел с плавающей запятой (избегание артефактов вида `55.00000000000001`).
  - List Comprehensions: компактный синтаксис `[expression for item in list if condition]` для генерации, фильтрации и преобразования типов списков в одну строку.
  - Slicing (срезы): извлечение подмножеств `list[start:stop]`, использование отрицательных индексов (`list[-3:]`), создание независимых копий списка через `[:]`.
  - Итерация по срезу: использование цикла `for` напрямую с срезом (`for item in list[:5]:`) без необходимости создавать промежуточные переменные.
- **Инженерные практики:** 
  - Строгое соблюдение правила "использовать только пройденные концепции": осознанный отказ от непройденных format specifiers (`:.2f`), сложных комбинаций `range` + `len` + slicing и `round()` без второго аргумента.
  - Обновление `mentor_prompt.md` и `project_state.md`: добавлены явные запреты на использование непройденного синтаксиса без предварительного объяснения.
  - Атомарные коммиты и push каждого учебного скрипта (`step_39`, `step_41`, `step_43`, `step_45`) согласно `GIT_WORKFLOW.md`.
- **Связь с ML/AI (Зачем это нужно):** 
  - `min`/`max`/`sum` и `round`: базовые операции для анализа метрик (loss, accuracy) и предотвращения тихих ошибок (silent bugs) при сравнении float в логах и eval-скриптах.
  - List Comprehensions: стандарт индустрии для feature engineering, быстрой фильтрации датасетов и преобразования форматов данных (например, конвертация строк в float при загрузке CSV).
  - Slicing и копии `[:]`: критически важны для разделения данных на train/test/val выборки без случайной мутации исходного датасета (предотвращение data leakage).
  - Итерация по срезу: основа для обработки батчей (batches) данных или анализа только последних N эпох обучения модели.
- **Ссылка на коммит:** `feat(python): add matthes 4.3.3-4.4.1 (stats, list comprehensions, slicing, looping slice)`

### [2026-08-03] Этап 0: Завершение 3.4 (Index Errors) + Финальная синхронизация архитектуры курса
- **Изучено (Python/ML):** 
  - Ошибки индексации (`IndexError`): почему возникают (попытка доступа к несуществующему индексу) и как их предотвращать.
  - Использование `len()` для динамической проверки границ списка.
  - Отрицательная индексация (`-1`) как безопасный способ получения последнего элемента.
  - Условная проверка индекса (`if index < len(list)`) перед доступом.
- **Инженерные практики:** 
  - Полная синхронизация названий 5 проектов между `ROADMAP.md` и `COURSE_PLAN.md` (источник истины).
  - Обновление `README.md` и `mentor_prompt.md`: длительность Этапа 0 изменена на "4-5 недель (интегрированный)", добавлен реалистичный горизонт "12 месяцев".
  - Внедрение Google Colab для Проекта 5 (LLM Twin) и пунктов карьерной подготовки (резюме, LinkedIn, LeetCode, Kaggle) в Этапы 3-5.
  - Добавление жесткого 9-пунктного чек-листа проверки в `mentor_prompt.md` для предотвращения рассинхронизации файлов в будущем.
- **Связь с ML/AI (Зачем это нужно):** 
  - `IndexError` — одна из самых частых ошибок при обработке батчей, срезов датасетов или признаков. Понимание границ массивов критично для написания устойчивых ML-пайплайнов. Отрицательная индексация часто используется для быстрого доступа к последним предсказаниям или метрикам в логах.
- **Ссылка на коммит:** `feat(python): complete matthes 3.4 index errors; docs: sync roadmap, course plan, readme with 5-track integrated stage 0`

### [2026-08-03] Этап 0: Реструктуризация плана курса — интеграция внешних ресурсов
- **Изучено (Python/ML):**
  - **Стратегический аудит курса:** глубокий анализ текущего плана выявил 6 критических пробелов, которые делали курс несамодостаточным: отсутствие ML-теории изнутри (как работает градиентный спуск, loss, backprop), математики (линал, статистика, теорвер), pandas/numpy/matplotlib как отдельной темы, SQL, HTTP/API, продвинутого Git, Hugging Face экосистемы.
  - **Реструктуризация в 6 этапов** вместо 5: Этап 0 расширен до "Фундамент: Python Core + ML-основы + математика" (7-9 недель, позже скорректировано до 4-5 недель интегрированного подхода) с 5 колеями параллельного изучения.
  - **Интеграция 4 внешних ресурсов** (точечно, без дублирования книг):
    - Kaggle Learn: Pandas (4 часа) — интерактивный курс по DataFrame, фильтрации, groupby
    - SQLZoo (5-6 часов) — SELECT, JOIN, GROUP BY, индексы
    - 3Blue1Brown: Essence of Linear Algebra (15 видео, 3-4 часа) — визуализация векторов, матриц, dot product
    - Hugging Face NLP Course (10-12 часов) — tokenization, pipelines, fine-tuning (встроен в Этап 4)
  - **Финализация COURSE_PLAN.md** с 5 колеями на Этапе 0: Python (Matthes Ch.1-11), ML-теория (Huyen DMLS Ch.1-4), Python для DS (Kaggle + ментор), Математика (3Blue1Brown + ментор), Инженерные основы (SQLZoo + ментор).
- **Инженерные практики:**
  - Принято решение: курс полностью самодостаточен через 6 книг + ментор + 4 точечных внешних ресурса. Coursera ML Specialization отклонён как дублирующий Huyen DMLS.
  - Общее время курса увеличилось с 24-31 недель до ~28-35 недель (позже оптимизировано до 12 месяцев с интегрированным подходом), но закрыты фатальные пробелы.
  - Обновлены все 4 файла трекинга: `COURSE_PLAN.md`, `ROADMAP.md`, `LEARNING_JOURNAL.md`, `project_state.md`.
- **Связь с ML/AI (Зачем это нужно):**
  - **Без pandas/SQL** невозможно сделать даже Проект 1 (Wine Classifier) нормально — это 80% работы ML-инженера в реальности.
  - **Без математики** (линал, статистика) не понять cosine similarity в RAG, loss functions в fine-tuning, работу embeddings.
  - **Без HTTP/API** невозможен Проект 2 (FastAPI) и работа с LLM API (OpenAI, Anthropic).
  - **Без Hugging Face** невозможен Проект 5 (LLM Twin) — `transformers`, `datasets`, `tokenizers`, `accelerate` обязательны.
  - Интеграция точечных ресурсов вместо "вечного обучения" — баланс между полнотой и фокусом на проектах.
- **Ссылка на коммит:** `docs: restructure course plan with 4 external resources (Kaggle, SQLZoo, 3Blue1Brown, HF NLP Course)`

### [2026-08-02] Этап 0: Списки, циклы, range() + первый push на GitHub
- **Изучено (Python/ML):**
  - **Списки (Lists):** создание `[]`, индексация (включая отрицательную `-1`), модификация по индексу.
  - **Операции со списками:** добавление (`.append()`, `.insert()`), удаление (`del` как оператор языка, `.pop()` с возвратом значения, `.remove()` по значению).
  - **Организация списков:** постоянная сортировка (`.sort()`), временная безопасная сортировка (`sorted()`), обратный порядок (`.reverse()`), получение длины (`len()`).
  - **Циклы:** перебор элементов через `for`, критическая важность отступов (IndentationError, TabError) для определения блоков кода.
  - **Числовые последовательности:** функция `range(start, stop, step)`, понимание того, что `stop` не включается, использование отрицательного шага. Преобразование ленивого итератора `range` в полноценный список через `list()`.
  - **Разница `range` vs `list`:** `range` — ленивый (экономит RAM), `list` — в памяти (поддерживает все методы).
- **Инженерные практики:**
  - Создан и зафиксирован `GIT_WORKFLOW.md`: правила атомарных коммитов, стандарт Conventional Commits, частота фиксаций.
  - Выполнен переход на современный стандарт GitHub: переименование ветки `master` в `main`.
  - Совершен первый успешный `git push` в удалённый репозиторий `mitjagagin/ml-journey`.
  - Актуализирован `README.md` (статус, структура, прогресс, ссылка на GitHub).
  - Добавлен блок обновления README.md в локальный `flow.md`.
  - Зафиксировано правило: `flow.md` — локальный шаблон промпта для чата, НЕ коммитится в репозиторий.
- **Связь с ML/AI (Зачем это нужно):**
  - **Списки и циклы** — абсолютная основа для итерации по признакам (features) при масштабировании, перебора списка моделей для кросс-валидации, построчной обработки датасетов.
  - **`del` vs `.pop()` vs `.remove()`:** разные сценарии в ML-пайплайнах: `del` для удаления некорректного столбца-признака, `.pop()` для извлечения и использования последней модели из очереди инференса, `.remove()` для очистки конфига от устаревших параметров.
  - **`.sort()` vs `sorted()`:** правило большого пальца — если не уверены, используйте `sorted()` (безопаснее, не мутирует исходный датасет).
  - **`len()`** — критично для проверки размера датасета, количества признаков, размера батча перед подачей в модель.
  - **`range` vs `list(range)`:** `range` критически важен для экономии RAM при генерации миллионов индексов эпох или батчей. `list(range())` необходим, когда требуется индексация, взятие срезов (slicing) или передача коллекции в функции sklearn/pandas.
  - **Первый push на GitHub** — начало формирования публичного портфолио AI Engineer, защита кода от потери.
- **Ссылка на коммит:** `docs: update README.md, add range tutorials, establish GIT_WORKFLOW.md`

### [2026-07-31] Этап 0: Завершение Главы 2 Matthes — Строки, Числа и Философия Python
- **Изучено (Python/ML):**
  - Завершение темы строк (2.2.6): правильное комбинирование одинарных/двойных кавычек, экранирование апострофов (`\'`), многострочные строки (`"""`) для промптов.
  - Числовые типы: `int` и `float`, разница между обычным делением `/` (всегда возвращает `float`) и целочисленным `//` (возвращает `int`).
  - Продвинутые фишки: подчеркивания в больших числах (`1_000_000`), множественное присваивание (`a, b, c = 1, 2, 3`).
  - Константы по PEP 8: именование `ЗАГЛАВНЫМИ_БУКВАМИ` для гиперпараметров и путей.
  - Профессиональные комментарии: объяснение "почему", а не "что".
  - Философия Python: `import this` — "Readability counts", "Simple is better than complex".
  - Функция `type()` и атрибут `.__name__` для получения имени типа объекта.
- **Инженерные практики:**
  - Разделение структуры проекта: `src/` для продакшен-кода, `tutorials/` для учебных скриптов.
  - Внедрены type hints (`: int`, `: float`, `: str`) и docstrings во всех учебных файлах.
  - Настроен `.gitignore` и осмысленные Git-коммиты.
  - Создание системы трекинга: `project_state.md` (контекст ИИ), `LEARNING_JOURNAL.md` (портфолио), `COURSE_PLAN.md` (детальный план с галочками), `ROADMAP.md` (стратегия проектов).
  - Обновление локального `flow.md` промптами для синхронизации состояния между сессиями.
- **Связь с ML/AI (Зачем это нужно):**
  - Корректный синтаксис кавычек и экранирование критичны при формировании JSON-ответов, промптов для LLM и SQL-запросов.
  - Разница между `/` и `//` предотвращает тихие баги (silent bugs) при расчёте индексов, batch size или разбиении датасетов на фолды.
  - Константы — стандарт оформления гиперпараметров (`LEARNING_RATE`, `MAX_EPOCHS`), защищающий код от случайных изменений.
  - Философия Python ("Readability counts", "Simple is better than complex") — компас при проектировании читаемых и поддерживаемых ML-пайплайнов.
- **Ссылка на коммит:** `feat: complete matthes chapter 2, add course plan and roadmap`

### [2026-07-31] Этап 0: Подготовка окружения, Git и основы Python (Matthes Ч.2)
- **Изучено (Python/ML):**
  - Переменные как ярлыки (labels) в памяти, а не контейнеры. Разница между равенством (`==`) и идентичностью (`is`), функция `id()`.
  - Строки: методы изменения регистра (`.title()`, `.upper()`, `.lower()`), f-strings для форматирования.
  - Служебные символы: табуляция (`\t`), перенос строки (`\n`).
  - Очистка данных (Data Cleaning): методы `.strip()`, `.lstrip()`, `.rstrip()` и безопасное удаление префиксов `.removeprefix()`.
  - Обработка ошибок: чтение traceback, понимание `NameError`.
- **Инженерные практики:**
  - Настроена профессиональная структура проекта: разделение `src/` (продакшен-код) и `tutorials/` (учебные скрипты).
  - Внедрены type hints (`: str`, `: float`) и docstrings с первого шага (стандарт PEP 8).
  - Настроен `.gitignore` (исправлены скрытые пробелы, добавлены `.idea/`, `__pycache__/`).
  - Внедрён процесс управления контекстом сессий через `project_state.md` и ведение инженерного портфолио через `LEARNING_JOURNAL.md`.
- **Связь с ML/AI (Зачем это нужно):**
  - Очистка строк от пробелов и префиксов критична при парсинге сырых данных (CSV, JSON, логи) и подготовке текстов для NLP/LLM (например, очистка путей к файлам или именам артефактов).
  - Понимание `==` vs `is` предотвращает критические баги при передаче изменяемых объектов (например, pandas DataFrame или списков) между функциями ML-пайплайна.
  - Умение читать traceback — базовый навык отладки ML-моделей, API и инфраструктуры (Docker, CI/CD).
- **Ссылка на коммит:** `refactor: move tutorials to separate folder, clean src/`