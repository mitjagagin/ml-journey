# 📚 Learning Journal: ML/AI Engineering Journey

**Студент:** Дмитрий  
**Цель:** Junior/Middle AI Engineer  
**Стэк:** Windows, PyCharm, Miniconda, Git, Docker  

---

## 📝 Лог прогресса

### [2026-07-31] Этап 0: Подготовка окружения и основы Python
- **Инженерные практики:** 
  - Настроена структура проекта: разделение `src/` (продакшен) и `tutorials/` (обучение).
  - Настроен `.gitignore` для игнорирования `.idea/`, `__pycache__/`, `venv/`.
  - Внедрены type hints (`: str`, `: float`) и docstrings с первого шага (стандарт PEP 8).
- **Python Basics:**
  - Переменные как ярлыки (labels), а не коробки. Функция `id()` для проверки адресов в памяти.
  - Разница между равенством (`==`) и идентичностью (`is`).
  - Работа со строками: методы регистра (`.title()`, `.upper()`, `.lower()`), f-strings.
  - Служебные символы: табуляция (`\t`), перенос строки (`\n`).
  - Очистка данных (Data Cleaning): методы `.strip()`, `.lstrip()`, `.rstrip()`.
  - Чтение traceback (трассировки ошибок) и обработка `NameError`.
- **Git:**
  - Рефакторинг структуры, работа с untracked files, осмысленные коммиты.
- **Ссылка на коммит:** `docs: update learning journal with full python basics coverage`

### [Ожидает заполнения] Этап 1: Числа, комментарии и первый ML-проект
- ...