# 📖 Словарь терминов (GLOSSARY)

Централизованный словарь ML/AI-терминов, встречающихся в курсе.

Единственный источник истины для определений (принцип SSOT).

## Как обновлять

`GLOSSARY.md` обновляется только:

- в конце сессии;
- при закрытии главы или крупного блока;
- по явной просьбе Дмитрия.

Во время микро-шагов файл `GLOSSARY.md` не обновляется, а таблица черновика словаря не выводится после каждого шага.

Во время сессии ментор внутренне копит термины-кандидаты.

### Что добавлять в словарь

В `GLOSSARY.md` добавляются только термины, которые были реально разобраны в микро-шаге, то есть:

- термин был объяснён простыми словами;
- термин был частью блока «Новые концепции»;
- Дмитрий получил не просто название, а смысл и пример применения.

Если термин был упомянут только обзорно или как будущий контекст, он не добавляется сразу.

В таком случае ментор пишет:

> Термин упомянут обзорно, подробно разберём позже.

Не добавлять термины, которые были просто перечислены списком без нормального объяснения.

### Порядок обновления в конце сессии

В конце сессии ментор:

1. Выдаёт список терминов-кандидатов за сессию.
2. Разделяет:
   - термины, реально разобранные и готовые к добавлению;
   - термины, упомянутые обзорно и не добавляемые пока.
3. Просит Дмитрия вставить актуальный `GLOSSARY.md`.
4. Проверяет дубликаты:
   - если термина нет — добавляет новую строку;
   - если термин уже есть — предлагает уточнить существующее определение или пример.
5. После согласования выдаёт полный обновлённый файл.
6. Таблица терминов сортируется по принятому правилу файла.

## Правило сортировки

Таблица сортируется по первому видимому слову в колонке `Термин`.

Правило текущего файла:

1. Сначала термины, начинающиеся с латиницы: `A–Z`.
2. Затем термины, начинающиеся с кириллицы: `А–Я`.

Примеры:

- `**Drift** (дрейф данных)` сортируется по `Drift`.
- `**Бизнес-цель** (Business Objective)` сортируется по `Бизнес-цель`.

## Термины

| Термин | Простое определение | Пример / Контекст |
|---|---|---|
| **Adaptability** (адаптируемость) | Способность системы быстро перестраиваться под изменения в данных или бизнес-логике. | Модель переобучается на свежих данных при обнаружении drift. |
| **Baseline** (Базовая модель) | Простая модель, с которой сравнивают более сложные модели для оценки их реальной пользы. | Модель B (baseline) оказалась лучше для продакшена, чем сложная Model A, из-за скорости и дешевизны. |
| **Column-Major** (колонковое хранение) | Формат хранения данных, при котором все значения одной колонки (признака) хранятся вместе. Оптимален для чтения отдельных признаков при обучении модели. | В формате Parquet данные хранятся по колонкам; при чтении только одного признака не загружается весь датасет. |
| **Data Format** (формат данных) | Способ представления данных на диске: текстовый (читается человеком, но избыточный) или бинарный (компактный, быстрый для машин). | CSV и JSON — текстовые; Parquet и Avro — бинарные. |
| **Data Model** (модель данных) | Способ организации данных и связей между ними: таблицы, документы, графы и т.д. Определяет, как извлекать признаки для обучения модели. | Реляционная модель (таблицы), документная модель (вложенные документы), графовая модель (узлы и рёбра). |
| **Data Source** (источник данных) | Место или система, откуда ML-система получает данные для обучения и работы. Определяет формат, частоту и качество данных. | Клики пользователей, логи сервера, открытые датасеты на Kaggle, API погодных сервисов. |
| **Data-driven подход** (Data) | Решение задачи через обучение модели на данных. Модель сама находит закономерности. | Классификатор изображений, обученный на 1 млн фото кошек и собак. |
| **Document Model** (документная модель) | Модель данных, где информация хранится как гибкие вложенные документы (подобные JSON). Связи встроены в документ. Пример: MongoDB. | Профиль пользователя с вложенными данными о городе и списке хобби хранится как один документ. |
| **Drift** (дрейф данных) | Изменение распределения входных данных со временем, из-за чего качество модели падает. | После пандемии модель предсказания спроса начала ошибаться из-за изменения поведения покупателей. |
| **ETL** (Extract, Transform, Load) | Трёхэтапный процесс подготовки данных: извлечение из источника, преобразование (очистка, агрегация), загрузка в целевое хранилище. | Сырые заказы извлекаются из операционной БД, агрегируются по пользователям и загружаются в аналитическое хранилище для обучения модели. |
| **Iterative process** (итеративный процесс) | Циклическая разработка ML-системы, где после каждого этапа возможен возврат к предыдущему на основе новых данных или результатов. | После деплоя модель возвращается к этапу сбора данных из-за обнаруженного drift. |
| **Latency** (Задержка) | Время между запросом и ответом модели. В продакшене часто есть жёсткий лимит (например, не более 100 мс). | Требование бизнеса, чтобы модель предсказывала отток клиента не дольше 50 мс. |
| **Maintainability** (сопровождаемость) | Легкость обновления, мониторинга и передачи системы между инженерами и командами. | ML-пайплайн покрыт тестами, имеет версию модели и документацию в репозитории. |
| **ML-система** (ML System) | Программы, где логика не прописана вручную, а извлечена (обучена) из данных. Вероятностны: выдают оценку уверенности и могут ошибаться. Требуют мониторинга данных. | Фильтр, который суммирует веса слов "купите", "срочно" и "бесплатно", чтобы оценить вероятность спама. |
| **ML-цель** (ML Objective) | Конкретная математическая или алгоритмическая задача, которую решает модель, чтобы помочь достичь бизнес-цели. | "Спрогнозировать точное время доставки заказа". |
| **OLAP** (Online Analytical Processing) | Аналитическая база данных, оптимизированная на быстрое чтение и агрегацию больших объёмов исторических данных. Отсюда берут датасеты для обучения моделей. | ClickHouse, BigQuery, Snowflake — типичные OLAP-системы для аналитики и подготовки признаков. |
| **OLTP** (Online Transaction Processing) | Операционная база данных, оптимизированная на быструю запись текущих транзакций. Здесь живут сырые данные приложения до их обработки. | Оформление заказа в интернет-магазине записывается в OLTP-систему (например, PostgreSQL) в реальном времени. |
| **Production-ограничения** | Нефункциональные требования к ML-системе: скорость, стоимость, надёжность, масштабируемость. | Модель с точностью 95% не берут в продакшен, потому что она отвечает 5 секунд (не проходит по latency). |
| **Relational Model** (реляционная модель) | Модель данных, где информация хранится в таблицах с фиксированной схемой; связи между таблицами через ключи. Пример: PostgreSQL, MySQL. | Таблица заказов и таблица пользователей связаны через ключ user_id. |
| **Reliability** (надёжность) | Способность ML-системы корректно работать как на ожидаемых, так и на неожиданных входных данных. | Модель предсказания оттока возвращает валидный ответ даже при пропусках в данных клиента. |
| **Row-Major** (строковое хранение) | Формат хранения данных, при котором каждая запись хранится целиком, одна за другой. Оптимален для чтения отдельных объектов. | В формате CSV каждая строка файла — это одна полная запись датасета. |
| **Rule-based подход** (Mind) | Решение задачи через явные правила, написанные инженером. Быстро и понятно, но не масштабируется. | Спам-фильтр на ключевых словах: "бесплатно", "выигрыш". |
| **Scalability** (масштабируемость) | Способность системы сохранять качество и скорость работы при росте нагрузки или объёма данных. | Сервис рекомендаций обрабатывает 10 000 запросов в секунду без деградации latency. |
| **System-Generated Data** (системные данные) | Данные, которые система собирает автоматически: логи, метрики, транзакции. Обычно структурированы и высокого качества. | Логи сервера, показатели CPU и памяти, записи о транзакциях в базе данных. |
| **Trade-off** (компромисс) | Ситуация, когда улучшение одного параметра ухудшает другой (например, скорость vs качество). | Trade-off между latency (быстрый ответ) и accuracy (точность модели). |
| **User-Generated Data** (пользовательские данные) | Данные, создаваемые действиями людей: клики, запросы, отзывы, загрузки. Часто неструктурированы и содержат шум. | Клики и просмотры на сайте, отзывы на товары, загруженные фотографии. |
| **Бизнес-метрика** (Business Metric) | Метрика, измеряющая ценность для компании: прибыль, удержание клиентов, затраты или другой бизнес-результат. Отличается от ML-метрики, которая измеряет качество модели. | MRR (Monthly Recurring Revenue) — ежемесячная повторяющаяся выручка. |
| **Бизнес-цель** (Business Objective) | Конечный результат, важный для компании (например, рост прибыли, снижение затрат, удержание клиентов). | "Снизить затраты на логистику на 10%". |
| **Детерминированный vs Вероятностный** | Детерминированный подход дает точный ответ (Да/Нет). Вероятностный подход (ML) дает оценку уверенности (score), которую мы сравниваем с порогом (threshold). | Традиционный код возвращает просто "Спам". ML-код возвращает "Спам (оценка: 1.3)". |
| **Классификация** (Classification) | Тип ML-задачи, где модель относит объект к одной из заранее известных категорий. | Определение, является ли транзакция мошеннической (0 = нормальная, 1 = фрод). |
| **Метка** (Label) | Правильный ответ, который мы хотим, чтобы модель научилась предсказывать на основе признаков. | Слово «Комфортно» — это метка для температуры 15.0. |
| **Метрика** (Metric) | Числовая оценка качества работы ML-модели, которую мы оптимизируем в процессе обучения и отслеживаем в продакшене. | MAE (Средняя абсолютная ошибка), ROC-AUC, Accuracy. |
| **Модель** (Model) | Результат обучения алгоритма. Функция, которая принимает новые признаки и выдаёт предсказания. | После обучения на исторических данных модель может предсказать, будет ли холодно при температуре -5.0. |
| **Обучение** (Training) | Процесс, в ходе которого алгоритм анализирует исторические данные (признаки и метки) и создаёт модель. | Алгоритм посмотрел на 4 примера температур и меток, и выучил закономерность. |
| **Обучение без учителя** (Unsupervised Learning) | Тип ML, где модель ищет скрытые паттерны в данных без готовых правильных ответов (меток). | Кластеризация клиентов по поведению на сайте для сегментации. |
| **Обучение с подкреплением** (Reinforcement Learning) | Тип ML, где агент учится принимать решения в среде, получая награды или штрафы за свои действия. | Обучение дрона оптимальному маршруту доставки через систему наград. |
| **Обучение с учителем** (Supervised Learning) | Тип ML, где модель обучается на размеченных данных: есть входные признаки и правильные ответы (метки). | Предсказание цены дома по его площади и району на основе исторических данных. |
| **Признак** (Feature) | Входная характеристика объекта (данные), которую мы подаём в программу или модель для анализа и принятия решения. | Температура воздуха 15.0 — признак для предсказания погоды; слова «купите», «срочно», «бесплатно» — признаки для оценки спама. |
| **Ранжирование** (Ranking) | Тип ML-задачи, где модель упорядочивает объекты по степени релевантности или важности. | Поисковая выдача Google: упорядочивание страниц по релевантности запросу. |
| **Регрессия** (Regression) | Тип ML-задачи, где модель предсказывает непрерывное числовое значение. | Предсказание стоимости квартиры (например, 150000.50). |
| **Рекомендации** (Recommendation) | Тип ML-задачи, где модель предлагает пользователю релевантные объекты на основе его поведения или предпочтений. | Рекомендации фильмов на Netflix или товаров на Amazon. |
| **Снижение размерности** (Dimensionality Reduction) | Тип ML-задачи, где модель сжимает множество признаков в меньшее число с сохранением важной информации. | PCA для визуализации 1000-мерных данных в 2D-графике. |
| **Традиционное ПО** (Traditional Software) | Программы, где логика работы жестко задана разработчиком через правила (if/else). Детерминированы: при одних и тех же входных данных всегда выдают один и тот же результат. | Фильтр, который помечает письмо как спам только если в нем есть ТОЧНАЯ фраза "купите бесплатно". |
| **Фрейминг** (Framing) | Процесс перевода абстрактной бизнес-проблемы на язык конкретных ML-задач и метрик. | Перевод задачи "показать лучшие товары" в задачу "ранжирование" (Ranking). |
| **Целевая функция** (Objective Function) | Функция, которую модель минимизирует или максимизирует во время обучения. Она задаёт математическую цель оптимизации модели. | Модель может оптимизировать целевую функцию для классификации, а бизнес при этом оценивает результат через выручку или удержание клиентов. |
| **Эвристика** (Heuristic) | Практическое правило или чек-лист для принятия решений, когда точный алгоритм отсутствует. В ML используется для первичной оценки целесообразности задачи. | Чек-лист из 3 пунктов (сложность паттерна, наличие данных, толерантность к ошибкам) для решения, нужен ли здесь ML. |